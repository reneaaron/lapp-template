<% if(!user) { %>
  <div class="px-4 py-5 my-5 text-center">
    <img class="d-block mx-auto mb-4" src="/apple-touch-icon.png" alt="" width="72" height="57">
    <h1 class="display-5 fw-bold">Your project's tagline</h1>
    <div class="col-lg-6 mx-auto">
      <p class="lead mb-4">👉 Your elevator pitch goes here.</p>
    </div>
  </div>
<% } else { %>
  <div class="px-4 py-5 my-5 text-center">

      <h1 class="display-5 fw-bold">Welcome, <%= user.id.substring(0, 10) %>!</h1>
      <p class="lead">
        🔐 Only signed in users will see this section.
      </p>
  </div>
<% } %>